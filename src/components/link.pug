mixin Link(text, path= '#')
  a(href= path)= text

  mixin Card(title, edscr, link, linkText, cls)
    .card(class= cls)
    h1.card_title=title
    p.card_description= descr
    +Link(linkText, link)